server:
  port: 8761
spring:
  application:
    name: discovery-server

# Standalone server (no self-registration)
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    wait-time-in-ms-when-sync-empty: 0
